#+TITLE: resumel: A resume template package for Emacs Org Mode
#+AUTHOR: Barry Ridge
#+EMAIL: barry@barr.ai
#+DATE: \today

[[https://github.com/barryridge/resumel/actions/workflows/test.yml][https://github.com/barryridge/resumel/actions/workflows/test.yml/badge.svg]]

* Overview

=resumel= is an Emacs package for creating professional resumes and CVs using Org Mode and LaTeX templates. It supports four templates:

- *[[https://github.com/moderncv/moderncv][moderncv]]*: Classic, clean template with multiple style and colour options.
- *[[https://github.com/liantze/AltaCV][altacv]]*: Modern two-column layout.
- *[[https://github.com/posquit0/Awesome-CV][awesomecv]]*: Sleek template with strong typographic hierarchy.
- *modaltacv*: Custom variant combining the ModernCV aesthetic with AltaCV's two-column layout.

Templates are modular: each lives in its own directory with a =.el= file (LaTeX class definition) and a =.org= file (macros). Common macros are shared via =resumel.org=. Swap templates with a single keyword.

* Template Gallery

#+begin_export html
<table>
  <tr>
    <td align="center"><strong>moderncv</strong></td>
    <td align="center"><strong>altacv</strong></td>
  </tr>
  <tr>
    <td><img src="docs/previews/moderncv.png" alt="moderncv template preview" width="400"/></td>
    <td><img src="docs/previews/altacv.png"   alt="altacv template preview"   width="400"/></td>
  </tr>
  <tr>
    <td align="center"><strong>awesomecv</strong></td>
    <td align="center"><strong>modaltacv</strong></td>
  </tr>
  <tr>
    <td><img src="docs/previews/awesomecv.png"  alt="awesomecv template preview"  width="400"/></td>
    <td><img src="docs/previews/modaltacv.png"  alt="modaltacv template preview"  width="400"/></td>
  </tr>
</table>
#+end_export

* Prerequisites

- *Emacs* 26.1+  [[https://www.gnu.org/software/emacs/download.html][Download Emacs]]
- *LaTeX distribution*: [[https://www.tug.org/texlive/][TeX Live]] / [[https://miktex.org/][MiKTeX]] / [[http://www.tug.org/mactex/][MacTeX]]
- *diff-pdf*: required for tests  [[https://github.com/vslavik/diff-pdf][diff-pdf GitHub]]

* Installation

#+html: <details><summary>
** Vanilla Emacs
#+html: </summary>

#+begin_src shell
git clone https://github.com/barryridge/resumel.git
#+end_src

#+begin_src elisp
(add-to-list 'load-path "/path/to/resumel/")
(require 'resumel)
;; optional key binding:
(global-set-key (kbd "C-c r") 'resumel-export)
#+end_src

#+html: </details>

#+html: <details><summary>
** Doom Emacs
#+html: </summary>

In =packages.el=:

#+begin_src elisp
(package! resumel
  :recipe (:host github :repo "barryridge/resumel"
           :files ("*.el" "*.org" "templates")))
#+end_src

Run =doom sync=, then in =config.el=:

#+begin_src elisp
(use-package resumel
  :config (require 'resumel))
;; optional key binding:
(map! :global "C-c r" #'resumel-export)
#+end_src

#+html: </details>

#+html: <details><summary>
** Spacemacs
#+html: </summary>

Add =resumel= to =dotspacemacs-configuration-layers= and press =SPC f e R=.

#+html: </details>

#+html: <details><summary>
** Other Emacs Distributions
#+html: </summary>

Clone the repository, add it to your load path, and require the package.

#+html: </details>

* Usage

** Quick Start

1. Select a template:

   #+begin_src
   M-x resumel-select-template RET moderncv RET
   #+end_src

   This writes =#+RESUMEL_TEMPLATE: moderncv= into the buffer header and opens a
   side-window PDF preview of the template (requires PDF files in
   =resumel-preview-pdf-dir=; see [[*Template Previews][Template Previews]] below).

2. Write your resume using the provided macros (see =tests/fixtures/= for examples).

3. Export to PDF:

   #+begin_src
   M-x resumel-export RET
   #+end_src

** Template Previews

When =resumel-select-template= is called, resumel shows a live PDF preview in a
right side window as you scroll through the template candidates.  The preview
updates with each candidate and is automatically removed once you confirm or
cancel the selection.

By default, previews are *enabled* (=resumel-show-preview-on-select= is =t=) and
resumel looks for sample PDFs in the =tests/expected/= directory of the package
installation.  If you clone the full repository and run the tests, the PDF files
will already be there.

To disable previews:

#+begin_src elisp
(setq resumel-show-preview-on-select nil)
#+end_src

To point resumel at a custom directory of sample PDFs (e.g. your own previously
exported resumes):

#+begin_src elisp
(setq resumel-preview-pdf-dir "/path/to/my/sample-resumes/")
#+end_src

PDF files in that directory should be named =<template>-*.pdf= (e.g.
=moderncv-mine.pdf=, =altacv-mine.pdf=).

resumel selects among multiple matches using =resumel-preview-pdf-pattern=, a
regexp matched against the filename.  The default =-complex\.pdf$= prefers the
more detailed fixture PDFs.  Override to taste:

#+begin_src elisp
(setq resumel-preview-pdf-pattern "-basic\\.pdf$")  ; prefer basic PDFs
(setq resumel-preview-pdf-pattern "")               ; first alphabetically
#+end_src

The preview buffer uses whatever PDF viewer Emacs has configured: =pdf-view-mode=
(pdf-tools) if installed, or =doc-view-mode= otherwise.

** Template Variables

Override any template variable by adding a =#+RESUMEL_<VAR>: value= keyword to your Org file header:

#+begin_src org
#+RESUMEL_TEMPLATE: moderncv
#+RESUMEL_MODERNCV_COLOR: green
#+RESUMEL_GEOMETRY: scale=0.8, top=1.5cm, bottom=1.5cm
#+end_src

*** Browsing variables

=show-X= commands show only buffer-set variables; =show-all-X= commands show all variables including defaults:

| Command                                   | Scope                  | Shows          |
|-------------------------------------------+------------------------+----------------|
| =M-x resumel-show-variables=              | Core + template        | Set only       |
| =M-x resumel-show-all-variables=          | Core + template        | All (defaults) |
| =M-x resumel-show-core-variables=         | Shared (all templates) | Set only       |
| =M-x resumel-show-all-core-variables=     | Shared (all templates) | All (defaults) |
| =M-x resumel-show-template-variables=     | Current template only  | Set only       |
| =M-x resumel-show-all-template-variables= | Current template only  | All (defaults) |

This opens a =*resumel: template variables*= buffer with overridden values highlighted. Edit variables directly in the buffer — changes sync immediately back to the Org file. Press =C-c r= to refresh, =q= to close.

*** Getting and setting individual variables

#+begin_src
M-x resumel-get-template-variable RET MODERNCV_COLOR RET
M-x resumel-set-template-variable RET MODERNCV_COLOR RET burgundy RET
#+end_src

=resumel-get-template-variable= returns the effective value (buffer keyword if set, otherwise the template default). =resumel-set-template-variable= inserts or updates the =#+RESUMEL_<VAR>:= keyword.

** Viewing Template Files

To inspect a template's LaTeX class definition or available macros, open the
template source files directly:

#+begin_src
M-x resumel-view-template-el RET moderncv RET   ; open moderncv.el
M-x resumel-view-template-org RET moderncv RET  ; open moderncv.org
#+end_src

Both commands prompt with completion and default to the template currently
selected in the active Org buffer.  The file opens in another window via
=find-file-other-window=.

| Command                         | Opens                                               |
|---------------------------------+-----------------------------------------------------|
| =M-x resumel-view-template-el=  | =templates/<name>/<name>.el= — LaTeX class & defaults |
| =M-x resumel-view-template-org= | =templates/<name>/<name>.org= — Org macros             |
| =M-x resumel-view-template-pdf= | Sample PDF for the chosen template                  |

=resumel-view-template-pdf= also shows a live side-window preview while you
navigate candidates (like =resumel-select-template=), but does not change the
template selection in your Org file.

* Testing

#+begin_src shell
make test    # run all tests
make clean   # remove generated results
#+end_src

Tests export all fixture files and compare resulting PDFs against expected outputs using =diff-pdf=.

* Adding New Templates

1. Create =templates/newname/= with =newname.el= (LaTeX class) and =newname.org= (macros).
2. In =newname.el=, define =resumel-newname-variable-defaults= as a defaults alist.
3. Add ="newname"= to the =resumel-default-template= choices in =resumel.el=.
4. Add a test fixture and expected PDF; run =make test=.

* Acknowledgements

=resumel= is inspired by and builds on Aidan Scannell's [[https://github.com/aidanscannell/my-org-resume][my-org-resume]].

Developed with the assistance of [[https://claude.ai][Claude]], [[https://chatgpt.com][ChatGPT]], [[https://github.com/karthink/gptel][gptel]], and [[https://github.com/xenodium/agent-shell][agent-shell]].

* License

=resumel= is licensed under the [[LICENSE][GNU GPL]].

* Contributing

Contributions are welcome. Fork the repository and submit pull requests with relevant tests.

* Contact

Please open an issue on the [[https://github.com/barryridge/resumel/issues][GitHub repository]].
